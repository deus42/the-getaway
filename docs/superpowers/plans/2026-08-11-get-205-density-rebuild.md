# GET-205 Density Rebuild Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox gates and must run inline in the visible shared `main` workspace; this plan does not authorize subagents, a branch, a worktree, staging, or a commit.

**Goal:** Replace the rejected sparse/dark GET-205 v4 city and rejected coverage-only first v5 greybox with a route-safe, source-traceable, mission-legible four-block v5 city that matches the locked blend's massing and wet blue-black treatment, removes random ambient NPC allocation, stages purposeful mission presence, and preserves executable topology.

**Architecture:** Keep the accepted GET-204 recipe and recoverable GET-205 v3/v4 sources immutable, and preserve the first v5 coverage recipe/render as rejected revision evidence. Gate the corrected v5 recipe through the versioned route/anchor/probe dataset, a mid-band building mask, occlusion-aware per-identity masks, and a fixed-camera five-question mission-legibility read. Require requester approval of the mission-staged material-free greybox before facade work, then approval of one blue-hour hero before regenerating the three-state schema-v2 runtime matrix. Publish only through complete staging and atomic replacement.

**Tech Stack:** Blender 5.0.1 Cycles/Metal, Python Blender scripts, Node.js contract/asset validators, Vite/React/TypeScript, Phaser, Jest, Yarn, Playwright browser evidence, Markdown canonical specifications, Linear MCP.

---

## Global execution constraints

- [ ] Work on visible shared `main`; do not pull, rebase, branch, create a worktree, or clean unrelated files.
- [ ] Before every edit group, inspect the current dirty tree and preserve unrelated GET-204, GET-208, GET-214, GET-216, documentation, runtime, generated evidence, and personal files.
- [ ] Use `apply_patch` for tracked file edits. Generated Blender/render output stays under ignored `.generated` or staging roots.
- [ ] Do not alter production art/runtime before the separately authorized documentation commit exists and has been read back.
- [ ] Do not stage or commit without fresh explicit requester authorization. Documentation and implementation require separate authorizations.
- [ ] The exact requester-authored 24-point fixture is recovered below. Do not present the massing gate until all 14 Class A outcomes pass unchanged and all 10 Class B points have fresh v5 observations recorded; freeze Class B expectations only with requester massing approval.
- [ ] Stop at each requester visual gate. Automated reports support the decision and never substitute for it.

## Task 1: Commit the canonical density-rebuild entry gate

**Files:**

- Modify: `memory-bank/01 MVP/12 Game Design Decision Register.md`
- Modify: `memory-bank/01 MVP/13 Level 0 Content and State Matrix.md`
- Modify: `memory-bank/01 MVP/14 Specification Review Queue.md`
- Modify: `memory-bank/01 MVP/15 Linear Implementation Program.md`
- Modify: `memory-bank/01 MVP/30 Art Direction (MVP).md`
- Modify: `memory-bank/01 MVP/31 GET-204 Visual Rebuild Quality Contract.md`
- Modify: `memory-bank/01 MVP/42 Surveillance, Security & Civilian Behavior.md`
- Modify: `memory-bank/01 MVP/80 Day-Night Cycle.md`
- Modify: `memory-bank/01 MVP/95 MVP Readiness Checklist.md`
- Modify: `memory-bank/04 Engineering/Architecture.md`
- Modify: `memory-bank/04 Engineering/Roadmap.md`
- Modify: `the-getaway/src/content/gameBible/traceability.ts` (decision classification only; preserve all pre-existing hunks)
- Modify: `progress/GET-205.md`
- Create: `docs/superpowers/plans/2026-08-11-get-205-density-rebuild.md`

- [ ] Record `GDR-ART-017`, `GDR-CIV-003`, and `GDR-SUR-013` with direct 2026-08-11 requester provenance.
- [ ] Record `OPEN-LAYOUT-006` for the recovered 24-point fixture's Class A/Class B disposition and `OPEN-ART-006` for the versioned building mask/two-sided density band.
- [ ] Amend `OPEN-CIV-001`, `OPEN-SEC-001`, and `OPEN-PERF-001` without falsely resolving GET-208 tuning or reusing v4 budgets.
- [ ] Rewrite the complete T5 section in `Linear Implementation Program.md`; publish that exact section as the full GET-205 Linear description and read it back for semantic parity.
- [ ] Keep GET-205 `In Progress`. Add a concise Linear comment naming the documentation gate, three requester gates, and the recovered 14-fixed/10-re-probed fixture disposition.
- [ ] Validate unique decision/open IDs, all 16 Art Direction sections, absence of active “geometry immutable” contradictions, and Markdown links.

Run:

```bash
git diff --check
rg -n "GDR-ART-017|GDR-CIV-003|GDR-SUR-013|OPEN-LAYOUT-006|OPEN-ART-006" memory-bank progress/GET-205.md
rg -n "preserve.*all sixteen|geometry.*remain.*fixed|topology-change: prohibited|3\.58 MB|1\.22 MB" memory-bank/01\ MVP memory-bank/04\ Engineering/Architecture.md progress/GET-205.md
```

Expected: new IDs occur once in their owning tables and are referenced consistently; stale statements are historical/realized-v4 labels only.

- [ ] Review only the intended documentation hunks against `HEAD` and the pre-edit dirty tree.
- [ ] Ask for explicit authorization for `docs(GET-205): authorize density rebuild`.
- [ ] If authorized, stage only the isolated GET-205 documentation/plan hunks, commit, verify configured human identity, read back the commit, and read back Linear again.

The original entry gate is complete at `c7ed7f4a67dd0509838a4328d885b5967bbe9c41`. The first v5 greybox was then rejected, so corrected production is blocked by this additional amendment gate:

- [ ] Record the rejected first-v5 recipe/render/metrics as recoverable failure evidence rather than overwriting or approving it.
- [ ] Amend `GDR-ART-017`, add `GDR-ART-018`, and replace the people-free interpretation of `GDR-CIV-003` with the bounded transit/café mission-stage population.
- [ ] Propagate the five-question legibility read, `0.95–1.05` density trial, at-least-ten/`0.005` identity floor, `0.15` identity cap, anchor mapping, and blend truth through every canonical T5 surface and the complete Linear description.
- [ ] Run documentation validation and exact Linear semantic readback; leave GET-205 `In Progress`.
- [ ] Ask for fresh authorization for the isolated amendment commit `docs(GET-205): stage mission-legible massing`. Do not alter the recipe, Blender scene, validators, actors, or runtime until that commit exists and is read back.

## Task 2: Recover and version the exact topology authority

**Files:**

- Create: `art/blender/get205/manifests/get205-preserved-topology-v1.json`
- Create: `art/blender/get205/manifests/get205-route-probes-v1.json`
- Create: `the-getaway/scripts/get205-density-route-graph.test.mjs`
- Create: `the-getaway/scripts/validate-get205-density-route-graph.mjs`
- Modify: `the-getaway/package.json`

- [ ] Inventory the current runtime IDs/coordinates for the two roads, three alleys, crossing, safehouse threshold/court, three traversal loops, and every proof/contact/entrance/terminal/hide/blend/objective/drone-launch anchor.
- [ ] Give each anchor an explicit clearance radius derived from its current interaction, actor, or traversal need; record provenance and source path.
- [ ] Version the recovered table below without changing labels, coordinates, stable-v4 results, `0.32` clearance, or provenance. Primary evidence: `/Users/deus/.claude/projects/-Users-deus-Projects-The-Getaway/68da2830-df5c-40ce-8c82-f5b6cb84bb3b.jsonl`, SHA-256 `40d9026a4d051e538203ca286d2cc058efd282996f343585745a7f9b391feb23`; Fable attachment readback: `/Users/deus/.codex/attachments/681fe4a1-e419-475e-9759-a6039cee0270/pasted-text.txt`, SHA-256 `94a23b0eeb612a451003e9860251333d3f17989cb75ceb2e86fee2f1e4935649`.

| # | Label | Point | Stable-v4 result | Class | V5 disposition |
|---:|---|---:|---|---|---|
| 1 | `interior.logistics.threshold` | `{33,17.5}` | rejected — occupied | A | Must remain rejected |
| 2 | `interior.public.market` | `{20,15}` | rejected — occupied | A | Must remain rejected |
| 3 | `interior.safehouse.corner` | `{22,36}` | rejected — occupied | A | Must remain rejected |
| 4 | `interior.service.utility` | `{50,28}` | rejected — occupied | A | Must remain rejected |
| 5 | `interior.safehouse.home` | `{7,37}` | rejected — occupied | A | Must remain rejected |
| 6 | `road.west` | `{10,22}` | accepted | A | Must remain accepted |
| 7 | `road.east` | `{50,22}` | accepted | A | Must remain accepted |
| 8 | `road.north` | `{29,5}` | accepted | A | Must remain accepted |
| 9 | `road.south` | `{29,40}` | accepted | A | Must remain accepted |
| 10 | `alley.logistics` | `{40,13.5}` | accepted | A | Must remain accepted |
| 11 | `alley.service` | `{40,34.5}` | accepted | A | Must remain accepted |
| 12 | `alley.safehouse` | `{10,31.5}` | accepted | A | Must remain accepted |
| 13 | `sidewalk.shelter` | `{24.2,20.8}` | rejected — occupied by `transit-a` | B | Re-probe without obsolete NPC pin |
| 14 | `gap.northofmarket.sidewalk` | `{20,20.6}` | accepted — consumable strip | B | Re-probe against v5 massing |
| 15 | `margin.corner` | `{0.5,0.5}` | accepted — consumable interstitial | B | Re-probe against v5 massing |
| 16 | `outside.west` | `{-2,22}` | rejected — outside district | A | Must remain rejected |
| 17 | `outside.south` | `{29,45.5}` | rejected — outside district | A | Must remain rejected |
| 18 | `blocker.securityguard` | `{31.25,21.7}` | rejected — occupied by actor blocker | B | Re-probe after guard removal/relocation |
| 19 | `shelter.a` | `{23.6,20.75}` | rejected — blocked surface | B | Re-probe against v5 massing |
| 20 | `shelter.b` | `{23.9,20.8}` | rejected — blocked surface | B | Re-probe against v5 massing |
| 21 | `shelter.c` | `{24.5,20.75}` | rejected — blocked surface | B | Re-probe against v5 massing |
| 22 | `shelter.d` | `{24.0,20.72}` | rejected — occupied by `transit-a` | B | Re-probe without obsolete NPC pin |
| 23 | `shelter.e` | `{23.5,20.9}` | accepted — consumable strip | B | Re-probe against v5 massing |
| 24 | `shelter.f` | `{24.6,20.9}` | rejected — blocked surface | B | Re-probe against v5 massing |

- [ ] Store `legacyExpected` for every point. Store immutable `expectedV5` only for Class A initially. Keep Class B `expectedV5: null` until the validator records an observed v5 result and the requester approves the greybox; then freeze the approved Class B outcomes in the fixture.
- [ ] Write the failing Node test first. It must reject a missing probe fixture, duplicate/missing invariant IDs, non-closed route polygons, a footprint/route intersection, a footprint/anchor-disc intersection, a broken loop, any changed Class A verdict, or any Class B point missing a fresh v5 observation at the massing gate. It must not fail merely because a Class B observation differs from its historical stable-v4 result.

Run:

```bash
cd the-getaway
node --test scripts/get205-density-route-graph.test.mjs
```

Expected before implementation: fail because validator/fixtures are absent.

- [ ] Implement deterministic polygon/segment, polygon/disc, loop-connectivity, and point-verdict checks. Do not infer pathfinding from render pixels.
- [ ] Add `art:level0:t5:massing:routes:validate` to `package.json`.

Run:

```bash
cd the-getaway
node --test scripts/get205-density-route-graph.test.mjs
yarn art:level0:t5:massing:routes:validate
```

Expected: topology structure and all 14 Class A outcomes pass against the unchanged accepted recipe; all 10 Class B historical outcomes can be reproduced as provenance but remain deliberately unfrozen for v5.

## Task 3: Lock new recipe/runtime identities and supersession contracts

**Files:**

- Create: `art/blender/get205/manifests/dense-four-block-v5.json`
- Create: `the-getaway/scripts/get205-density-rebuild-contract.test.mjs`
- Modify: `art/blender/get205/manifests/four-block-runtime-production.json` only when publishing the accepted v5 identity
- Modify: `the-getaway/src/game/level0/art/__tests__/get205ProductionRuntime.test.ts`
- Modify: `the-getaway/src/game/level0/art/__tests__/get205HidzuRuntime.test.ts`

- [ ] Write a failing contract test requiring recipe ID `get205-dense-four-block-v5`, runtime ID `get205-dense-four-block-production-v2`, immutable parent hashes for the accepted GET-204 and recoverable v3/v4 sources, 12–16 building identities, per-identity depth ownership, preserved-topology manifest linkage, and explicit invalidation targets.
- [ ] Test that no v5 manifest claims the prior “topology-change: prohibited” contract, but still rejects route/anchor/probe drift.
- [ ] Test that v4 identities remain recoverable historical records and cannot masquerade as v5.

Run:

```bash
cd the-getaway
node --test scripts/get205-density-rebuild-contract.test.mjs
```

Expected before manifest creation: fail.

- [ ] Add the minimal v5 manifest skeleton with `approval.massing: pending`, no facade/light/state outputs, and no production path.
- [ ] Keep runtime resolution on v4 until massing, hero, complete-matrix validation, and atomic publication all pass.

Run:

```bash
cd the-getaway
node --test scripts/get205-density-rebuild-contract.test.mjs
```

Expected: pass without changing the live runtime.

## Task 4: Build and validate the material-free v5 massing

**Files:**

- Create: `art/blender/get205/scripts/build_dense_four_block_v5.py`
- Create: `art/blender/get205/scripts/render_dense_four_block_v5_massing.py`
- Modify: `art/blender/get205/manifests/dense-four-block-v5.json`
- Generated only: `art/blender/get205/.generated/dense-four-block-v5/massing/`

- [ ] Import only named Neo Tokyo 2 sources and retain per-building root/material provenance. Start with source candidates such as `LargeB`, `MediumB`, `MediumI`, and compatible Small roots, but accept placements only through measured bounds and route validation.
- [ ] Preserve the rejected first-v5 manifest, scene hash, hero, route report, density report, and identity-contribution report as revision `coverage-r1`; it remains failure evidence and is never overwritten as the corrected candidate.
- [ ] Repack the four blocks into continuous street walls while keeping the declared route polygons and anchor discs clear. Split the two hero-dominant compounds into three or four independently source-fingerprinted parcels each with real seams. Use 12–16 individual identities and do not merge them into one wall object/cutout owner.
- [ ] Place the tallest Hidzu destination at `entrance.logistics.public`. Stage the checkpoint/guard-post/barrier at its base without intersecting the public road or entrance clearance; keep Needle's launch/patrol volume visibly associated with the approach.
- [ ] Add neutral, separately tagged greybox placeholders from actual anchors: transit shelter/bench/timetable; exactly two seated and one standing transit figure; café frontage/furniture with exactly two seated patrons; street-facing fixtures at all three terminals; recess geometry at all three hides; one public guard; and Needle. Validate every solid placeholder against routes and clearance discs. Figures, Needle, screens, and furniture remain outside the architecture density mask.
- [ ] Add a separately tagged outside-bounds perimeter backdrop with no collision, entrance, anchor, interaction, or density-mask contribution.
- [ ] Keep the fixed public-crossing camera registration. Assign neutral greybox materials only; disable facade dressing, labels, practical lights, emissives, and state variation. Mission figures are simple separately owned placeholders for composition and are never baked into production environment plates.
- [ ] Export building footprint polygons and identity/depth metadata, then run the route validator before rendering.

Run:

```bash
cd the-getaway
yarn art:level0:t5:massing:routes:validate
yarn art:level0:t5:massing:render
```

Expected: exact `1440×900` mission-staged greybox plus green topology report. Any route/probe/placeholder intersection failure aborts the render gate.

## Task 5: Add the fixed building-density metric

**Files:**

- Create: `art/references/get205/masks/kitbash-reference2-building-mask-v1.png`
- Create: `art/blender/get205/manifests/reference-delta-v2.json`
- Modify: `the-getaway/scripts/validate-get205-reference-delta.mjs`
- Modify: `the-getaway/scripts/get205-wet-rebake-merge.test.mjs`
- Create: `the-getaway/scripts/get205-density-metric.test.mjs`

- [ ] Write failing tests for fixed dimensions/registration, versioned mask hash, excluded roads/sky/HUD/actors/mission placeholders/perimeter backdrop, candidate/reference building fraction, lower and upper bounds, per-identity masks, meaningful-visibility threshold, largest-identity cap, and report image/hash integrity.
- [ ] Measure the reference fraction from `kitbash-reference2-blend-concept-v1.png`; do not hand-enter a desired candidate percentage.
- [ ] Implement the corrected provisional `0.95–1.05` candidate/reference ratio from `OPEN-ART-006`. At the fixed `720×450` analysis registration require at least ten occlusion-aware identity masks to contribute at least `0.005` of the full frame each and reject any identity above `0.15`. Declared recipe count never substitutes for rendered contribution; visual requester judgment remains authoritative.

Run:

```bash
cd the-getaway
node --test scripts/get205-density-metric.test.mjs scripts/get205-wet-rebake-merge.test.mjs
node scripts/validate-get205-reference-delta.mjs --mode massing
```

Expected: tests pass and the report records reference/candidate hashes, mask hash, fractions, ratio, and both bounds.

## Task 6: Stop for the requester massing gate

- [ ] Verify every comparison image opens from a real local path.
- [ ] Present one concise sheet: locked blend | corrected raw v5 greybox | building mask/delta; also include the per-identity segmentation and mission-legibility key.
- [ ] Present the route report with all invariant IDs, all 14 unchanged Class A verdicts, and fresh observed results for all 10 Class B points. Clearly distinguish historical Class B results from the proposed v5 expectations.
- [ ] State the building-fraction ratio/band, meaningful identity count, largest identity share, and any visible residual massing differences.
- [ ] Ask the requester to point to all five answers in the raw frame: where to go, what blocks the direct approach, where to blend, what to hack, and where to sneak. Labels on the diagnostic key cannot rescue a failed raw-frame read.
- [ ] Stop. Do not author facades, materials, lighting, states, cutouts, NPC/runtime changes, or publication until explicit requester massing approval.
- [ ] On approval, freeze the ten observed Class B outcomes as their v5 expectations, then record the accepted scene, recipe, footprint, topology-report, probe-fixture, mask, render, and comparison hashes in `progress/GET-205.md`, the manifest, and Linear.

## Task 7: Author facade identity, restricted-area presentation, and the blue-hour hero

**Files:**

- Create: `art/blender/get205/scripts/author_dense_four_block_v5_look.py`
- Create: `art/blender/get205/scripts/render_dense_four_block_v5_hero.py`
- Modify: `art/blender/get205/manifests/dense-four-block-v5.json`
- Create or extend: `art/blender/get205/manifests/reference-delta-v2.json`
- Generated only: `art/blender/get205/.generated/dense-four-block-v5/hero/`

- [ ] Add distinct facade identities without changing accepted massing: detailed warm and cold windows; readable labels; two to four cool-white/cyan fixtures; sparse red threat/warning points; one dominant large Hidzu Corporation landmark; barrier, warning lights, signage, public guard anchor, and Needle anchor at the restricted entrance.
- [ ] Keep cyan limited to declared devices and non-directional facade identity seams. Keep amber at windows/entrances/lamp falloff and red threat-specific.
- [ ] Lift the cool material/ambient floor so roads and facades retain detail at gameplay scale. Extend continuous wet sheen and physical reflection response under every visible warm/cool/red emitter. No bloom or post-render reflection compositing.
- [ ] Render Cycles/Metal at exact `1440×900`, centre `{29,22}`, zoom `2.9`, azimuth `45°`, elevation `30°`; use adaptive samples/denoising and inspect a real 200% road crop.
- [ ] Run shadow, amber, cyan, road-specular, luminance-spread, `p05`, source, camera, massing, and topology gates.
- [ ] Present reference | candidate | delta notes plus 200% crop and stop for requester hero approval.

## Task 8: Replace random ambient NPC allocation with bounded mission-stage population

**Files:**

- Modify: `the-getaway/src/game/level0/art/get204City.ts`
- Modify: `the-getaway/src/game/level0/art/__tests__/get204City.test.ts`
- Modify: `the-getaway/src/game/level0/city/civilianSchedules.ts`
- Modify: `the-getaway/src/game/level0/city/__tests__/civilianSchedules.test.ts`
- Modify only if already the owning seam: the existing blend-context availability resolver and its focused test

- [ ] Write failing tests requiring exactly two seated plus one standing transit passenger in declared nonblocking slots, exactly two seated café patrons at declared furniture, exactly the public restricted-area guard plus Needle, no static service-entrance guard, no random/wandering allocation, public-queue eligibility tied to actual group/schedule state, and absent-population unavailability for delivery activity.
- [ ] Remove the four static random civilians and random delivery/public-queue placement. Re-author only the fixed transit/café groups; keep actors runtime-owned and environment masters people-free.
- [ ] Preserve context IDs and future GET-208 behavior data. Do not add delivery population, detection, trespass, civilian reaction/pathfinding, or interception behavior; do not let café patrons create generic blend eligibility.

Run:

```bash
cd the-getaway
yarn test src/game/level0/art/__tests__/get204City.test.ts src/game/level0/city/__tests__/civilianSchedules.test.ts --runInBand
```

Expected: focused tests pass with exact slot ownership and blend truth; unrelated protected GET-208 partial work is untouched.

## Task 9: Bake all states and regenerate per-identity layers

**Files:**

- Create: `art/blender/get205/scripts/render_dense_four_block_v5_states.py`
- Modify: `the-getaway/scripts/get205-state-foregrounds.mjs`
- Modify: `the-getaway/scripts/get205-state-foregrounds.test.mjs`
- Modify: `the-getaway/scripts/build-get205-runtime-assets.mjs`
- Modify: `the-getaway/scripts/get205-runtime-assets.test.mjs`
- Modify: `the-getaway/scripts/get205-publication.mjs`
- Modify: `the-getaway/scripts/get205-publication.test.mjs`

- [ ] Write failing tests requiring three aligned `6400×3600` people-free masters, 12–16 identical per-building identity records in every state/profile, independent depth anchors, stable camera/crop registration, v5 geometry/source hashes, and absence of merged wall slices.
- [ ] Render dusk, blue hour, and curfew from one accepted v5 source. Preserve massing/registration byte-for-byte at metadata level while varying authored lighting/window occupancy only.
- [ ] Generate four desktop background tiles, one mobile background, and every per-identity foreground slice for each state.
- [ ] Rebuild collision, placement/depth metadata, state-specific stable hashes, position-pinning tests, fixed-frame masks, and accepted regression frames.
- [ ] Measure new per-state/profile compressed bytes, total transfer, one-set decoded residency, two-set transition peak, first load, and stable frame behavior. Do not compare acceptance to the invalid v4 ceiling.

Run:

```bash
cd the-getaway
node --test scripts/get205-state-foregrounds.test.mjs scripts/get205-runtime-assets.test.mjs scripts/get205-publication.test.mjs
yarn art:level0:t5:production
```

Expected: complete staging validation and atomic swap/readback; injected failure leaves the current public root intact.

## Task 10: Reconcile runtime identity, collision, and transition behavior

**Files:**

- Modify: `the-getaway/src/content/levels/level0/layoutContract.ts`
- Modify: `the-getaway/src/game/level0/art/get205HidzuRuntime.ts`
- Modify: `the-getaway/src/game/level0/art/get205LightingTransition.ts`
- Modify: `the-getaway/src/game/level0/scene/Level0Scene.ts`
- Modify focused tests under `the-getaway/src/game/level0/art/__tests__/`
- Modify: `the-getaway/src/game/level0/playtest/level0GateCoverage.ts`
- Modify: `the-getaway/src/game/level0/playtest/__tests__/level0GateCoverage.test.ts`

- [ ] Write failing tests for the new layout/runtime IDs, v5 collision/placement pins, invariant route/probe results, 12–16 depth slices, page-stable profile selection, current-phase-only preload, 19:50/21:50 prefetch, 20:00/22:00 boundary, 750 ms crossfade, generation-token stale rejection, failure retention/retry, and GameObject/texture disposal.
- [ ] Reconcile only the requester-approved v5 footprint/collision/anchor metadata. Do not move preserved anchors or weaken probes to fit art.
- [ ] Retain explicit `visualTreatment=get204-1` diagnostics. Do not run old/new production identities in parallel as silent fallbacks.
- [ ] Keep clock, shutter, surveillance, audio, actor tint, George, and HUD ownership unchanged.

Run:

```bash
cd the-getaway
yarn test src/game/level0/art/__tests__ src/game/level0/playtest/__tests__/level0GateCoverage.test.ts --runInBand
```

Expected: focused runtime/route suites pass.

## Task 11: Prove the complete live candidate

- [ ] Start the normal app at `http://127.0.0.1:5174/` and use the default GET-205 path; reserve `visualTreatment=get204-1` for diagnostics.
- [ ] Capture clean-world and current-HUD frames at `1440×900`, `1280×720`, and `1920×1080`; automatic mobile at `390×844`; overview; both profiles; three known occlusion sites; actor feet; and the `0.60→3.25` zoom sweep.
- [ ] Capture the fixed three-state strip and reference A/B. Verify dense mission-legible street walls, the Hidzu destination/checkpoint, detailed warm/cold windows, controlled colors, visual restricted area, unreachable backdrop, exact nonblocking transit/café groups, no random actors, truthful public-queue/delivery availability, public guard plus Needle, and unguarded service route.
- [ ] Exercise initial phase, both prefetches/boundaries, direct Wait jumps, Restart Attempt, hydration rewind, stale completion, failed load, retry, texture disposal, and one-/two-set residency without black frames or partial worlds.
- [ ] Run `yarn playtest:agent -- --ticket GET-205 --mode affected`; inspect the newest V2 report and address every in-scope actionable finding.
- [ ] Present the live side-by-side, overview, and mobile evidence. Stop for requester visual acceptance.

## Task 12: Close out only after requester acceptance

- [ ] Update realized manifest details, Art Direction, Architecture data flow, MVP Readiness, progress evidence, and the full Linear description/comment.
- [ ] Run all relevant asset/content validators.
- [ ] Run:

```bash
cd the-getaway
yarn lint
yarn build
yarn test --runInBand
yarn test --coverage --runInBand
yarn playtest:agent -- --ticket GET-205 --mode closeout
```

Expected: validators/lint/build/Jest pass, statement and line coverage remain above 80%, and the closeout AI Gamer report is `pass`.

- [ ] Inspect the complete diff, production asset inventory, outgoing payload/residency report, generated exclusions, and protected dirty files.
- [ ] Ask for explicit authorization for `improvement(GET-205): rebuild dense wet noir district`.
- [ ] If authorized, stage only the coherent accepted implementation/docs/assets, preserve human Git identity, commit, verify the committed build, and comment Linear with exact SHA/evidence.
- [ ] Leave GET-205 non-terminal until the requester verifies the committed build.
